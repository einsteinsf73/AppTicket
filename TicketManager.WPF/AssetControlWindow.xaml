<mah:MetroWindow x:Class="TicketManager.WPF.AssetControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicketManager.WPF"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="Controle de Patrimônio" Height="600" Width="1000"
        KeyDown="MetroWindow_KeyDown">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="HomeButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="🏠" Margin="5" Click="HomeButton_Click" ToolTip="Início"/>
            <Button x:Name="AddAssetButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="➕" Margin="5" Click="AddAssetButton_Click" ToolTip="Adicionar"/>
            <Button x:Name="EditAssetButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="✏️" Margin="5" Click="EditAssetButton_Click" ToolTip="Editar"/>
            <Button x:Name="DeleteAssetButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="❌" Margin="5" Click="DeleteAssetButton_Click" ToolTip="Excluir"/>
            <Button x:Name="HistoryButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="📜" Margin="5" Click="HistoryButton_Click" ToolTip="Histórico"/>
            <Button x:Name="ImportButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="📥" Margin="5" Click="ImportButton_Click" ToolTip="Importar"/>
            
            <TextBox x:Name="SearchTextBox" Width="300" Margin="10,5,5,5" mah:TextBoxHelper.Watermark="Pesquisar..." KeyDown="SearchTextBox_KeyDown"/>
            <Button x:Name="FilterButton" Style="{StaticResource MahApps.Styles.Button.Circle}" Content="🔍" Margin="5" Click="FilterButton_Click" ToolTip="Filtrar"/>
        </StackPanel>

        <DataGrid x:Name="AssetsGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource MahApps.Styles.DataGrid.Azure}" Sorting="AssetsGrid_Sorting">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item" Binding="{Binding Item}"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Brand}"/>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Model}"/>
                <DataGridTextColumn Header="N/S" Binding="{Binding SerialNumber}"/>
                <DataGridTextColumn Header="Patrimônio" Binding="{Binding AssetNumber}"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Value}"/>
                <DataGridTextColumn Header="Data da Compra" Binding="{Binding PurchaseDate, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTextColumn Header="Fornecedor" Binding="{Binding Supplier}"/>
                <DataGridTextColumn Header="NF" Binding="{Binding InvoiceNumber}"/>
                <DataGridTextColumn Header="Garantia" Binding="{Binding Warranty}"/>
                <DataGridTextColumn Header="Local" Binding="{Binding Location}"/>
                <DataGridTextColumn Header="Setor" Binding="{Binding Sector}"/>
                <DataGridTextColumn Header="Departamento" Binding="{Binding Department}"/>
                <DataGridTextColumn Header="Colaborador" Binding="{Binding Employee}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Manutenção" Binding="{Binding Manutencao}"/>
                <DataGridTextColumn Header="Previsão Manutenção" Binding="{Binding PrevisaoManutencao, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTextColumn Header="OBS" Binding="{Binding Observations}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</mah:MetroWindow>